apiVersion: v1
kind: Service
metadata:
  name: {{ include "stuent-app-deployment.fullname" . }}
  labels:
    {{- include "stuent-app-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stuent-app-deployment.selectorLabels" . | nindent 4 }}
